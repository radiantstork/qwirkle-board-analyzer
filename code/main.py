from run_project import get_config

# for i in range(4, 5):
#     get_config(data_type="train", game_index=i, move_index=0)
#     print(f"game {i} finished")

# get_config(data_type="test", game_index=1, move_index=0)


